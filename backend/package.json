{
  "name": "quizapp",
  "version": "1.0.0",
  "description": "Backend for quiz generator web app",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../frontend && npm run build && cp -r build ../backend",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tristan Shawn den Ouden",
  "license": "ISC",
  "dependencies": {
    "cookie-session": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "passport": "^0.6.0",
    "passport-google-oauth": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
